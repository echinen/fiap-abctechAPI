spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  datasource:
    password: sa
    username: sa
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false